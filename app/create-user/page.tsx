import UserForm from "../(components)/UserForm";

type Props = {};

const CreateUser = async (props: Props) => {
  return (
    <div>
      <UserForm></UserForm>
    </div>
  );
};

export default CreateUser;
